# 实验登记簿

每次上机训练在这里登记一条：**为什么跑、和谁比、改了什么、结果、结论、下一步**。
按时间顺序追加，旧条目只补结果、不改结论（结论错了就在新条目里更正并链回来）。

## 约定

- **命名**：`P<n>` 探测 / 冒烟性质的 run，`A`、`B`、`C`… 正式实验，按立项顺序取字母；取消的实验也留一行。
- **配置**：`configs/exp-<字母>-<短名>.toml`，只写和父实验不同的键，文件头注释写父实验与改动。
- **上机**：`bash scripts/run-exp.sh configs/exp-<字母>-<短名>.toml exp-<字母>`（自动选线程、配置副本放 `runs/`、终端日志 `runs/<名>.console.log`）。
- **一次只改一个变量**。要同时改两处，拆成两个实验或写明理由。
- **结果**：run 目录与 tar.gz 在 `runs/`（gitignore，不入库），这里记路径与关键数字。主判据是
  **评测集贪心指标**（`eval/<更新数>.json` 的 `overall`），训练时 `ep/*` 是采样策略、只作参考。
- **对比图**：`uv run --frozen python -m stgtrain.plots runs/<甲> runs/<乙> --out runs/cmp-<甲乙>`（横轴 env_steps = 游戏帧）。
- **噪声地板（2026-09-20 实测，见「更正：J 的代价是噪声」）**：同一条 run 内相邻评测点的撑过率自己就摆 **±3pp**，
  320 局的标准误约 1.5pp，`best.pt` 还是 14 次评测里取最大（选择偏差）。所以 **两条 run 的 best 对 best 差 ≤3pp 不算结论**。
  比撑过率要用**最后 4 次评测的均值**（每档 1280 局，名义标准误约 0.8pp）；`best.pt` 只用来挑部署模型。
  抖动类指标（方向/s、连击占比）的效应是几十个百分点，不受这条限制。
- **补测**：`uv run --frozen python -m stgtrain.eval_ckpt runs/<run>/checkpoints/best.pt [--all-cards --ranks 2,3]`。
  注意 CPU 补测和训练时的 GPU 评测同种子但浮点不同，撑过率会差 ±1–2 个百分点。
- **看操作**：`uv run --frozen python -m stgtrain.replay_gif runs/<run>/checkpoints/best.pt [--cards …] [--episodes 0,1]`
  → `runs/<run>/replay/<卡>_r<档>_e<局>.gif`，与评测同一局；黄圈 = 指令点、底部色带 = 最近 64 步方向键。

## 总表

| # | 日期 | 父 | 改动 | 帧 / 用时 | 评测撑过 | 跟点 | 方向变化/s | 结论 |
|---|---|---|---|---|---|---|---|---|
| P0 | 09-16 | — | 探测：6 张训练卡 + 1 张评测卡 × 16 局 | 0.68 亿 / 15 min | 0.50 | 0.56 | 11.4 | 这套 reward + 特征化 + PPO 能学 |
| A | 09-17 | P0 | 卡池换 TH06 1–3 关 45 张（33 起点）、评测 9 卡 × 32 局 | 5.24 亿 / 2h09 | **0.938** | **0.765** | 23.7 | **基线**；接近平台期，泛化无差距 |
| B | — | A | 小模型 d32/h4/t128 | — | — | — | — | **取消**（用户定只用当前尺度） |
| C | 09-17 | A | `frame_skip = 2`（gamma 0.990、hold 0.02 按秒对齐） | 5.24 亿 / 67 min | 0.872 | 0.659 | 13.5 | 不采用：同帧同墙钟都不如 A，降抖只是封顶 |
| D | 09-17 | A | `key_press = -0.001` | 5.24 亿 / 2h12 | 0.920 | 0.762 | 19.5 | shift 减半、方向几乎不降：模型看不到上一步方向 |
| E | 09-17 | D | 特征化 `danger_topk_v2`：观测加上一步动作 | 5.24 亿 / 2h11 | **0.931** | **0.771** | 13.0 | 方向 −33% 且不再来回跳，活命反而略好；仍非个位数 |
| F | 09-18 | E | `key_press = -0.003`（×3） | 5.24 亿 / 2h29 | **0.938** | 0.764 | **6.6** | **成功**：不靠滞回就打到 6–7/s，活命不掉，shift 0.75/s |
| G | — | F | 特征化 `danger_topk_v3`：敌人速度（按 id 差分） | — | — | — | — | **取消单独立项**（用户定：快速敌人撞自机的情形少），并进 G0 的默认 |
| G0 | 09-19 | F | **新卡池基线**：84 张卡 / 74 起点（+起点钳档）+ v3 默认 + 瞬移守卫 | 5.24 亿 / 1h59 | 0.912 | 0.749 | 8.8 | 卡池变难 −2.6pp（对 F）；u1400 后在 0.90–0.92 平台震荡 |
| H | 09-19 | G0 | 起点难度采样（死亡率 EMA 加权，两级钳位） | 5.24 亿 / 2h05 | **0.950** | **0.766** | 8.4 | **成功 +3.8pp**，且反超 F；曲线到 u2000 仍在爬 → **转默认** |
| I | 09-19 | G0 | 自由躲弹混合意图（跟点 60 / 锚点 30 / 自由 10） | 5.24 亿 / 2h05 | 0.934 | **0.772** | **5.99** | **抖动 −32% 且十张卡无一例外**，跟点率反而最高；不含课程 |
| I2 | 09-19 | H+I | 两者叠加 + 3500 轮 + 评测集加 rank 3 | 9.18 亿 / 5h20※ | r2 **0.947** / r3 **0.909** | **0.787** | 5.94 | 叠加成立；r3 比 H 高约 5pp，抖动保住 I 的水平 |
| J | 09-19 | I2 | `quick_change = -0.01`：只罚「方向保持 ≤3 帧就又换」 | 9.18 亿 / 5h25※ | r2 0.928 / r3 0.884 | 0.770 | **3.55** | **连击腰斩 42%→22%**；~~撑过代价 −2pp~~ → **代价是噪声**（末 4 次均值持平，见更正） |
| N1 | 待跑 | J | **手部运动层**：每段方向的最短保持帧数随机 `U{2..6}`，对模型不可见；特征化 v4 加「当前方向已保持几帧」 | — | — | — | — | 见「N1 / N2 手部运动层」 |
| N2 | 待跑 | N1 | N1 + 变向生效延迟随机 `U{0..2}` 帧 | — | — | — | — | 同上 |

> ※ I2/J 是同机并行跑的，墙钟含互相抢 GPU 的损耗（合计吞吐是单跑的 1.36 倍）。
>
> **三把尺子别混用**：A–F 用的是旧评测集（9 卡，含 2 张无弹卡）；G0–I 用第二版（10 卡 × rank 2）；
> I2 起用第三版（10 卡 × rank 2+3，20 组）。跨版本比要对**分卡**或 `by_rank.r2`，别对 `overall`。
> 详见「评测集沿革」。

## 阶段性总结（2026-09-20，A–J 十条跑完）

### 现在最好的配置

`configs/exp-j-quickchange.toml`（= `exp-i2-stack.toml` + `quick_change`；09-20 更正后 **J 转默认**，它在活命上没有可测的代价）：

- 特征化 `danger_topk_v3` = 危险度 top-K + 密度图 + **上一步动作**（E）+ **敌人速度**（G0 并入）
- **起点难度采样**（课程：死亡率 EMA 加权 + 两级钳位，H）
- **意图混合** `mixed_v1`：跟点 60 / 锚点 30 / 自由 10（I）
- reward：`death 10 · follow_shaping 1 · hold 0.01 · key_press −0.003`（J 再加 `quick_change −0.01`）
- 4096 env × 64 步 × 3500 轮 = 9.18 亿帧，单跑约 3h40（RTX 4090 + EPYC 7702P，threads 32）

### 三条主线各自走到哪

**① 活命**：A 0.938（45 卡池 / 9 卡评测）→ 卡池扩到 84 张后跌到 G0 0.912 → 课程采样补回 H 0.950 →
I2 在新尺子上 **r2 0.947 / r3 0.909**。**训练分布内已接近这套卡池的上界**，继续投入主要买 r3（未见难度）。

**② 抖动（拟人）——收益最大的一条**：方向变化 A 26.5/s → F 7.54 → I 5.99 → **J 3.55**
（平均每 17 帧 = 0.28 s 换一次方向，进入人类区间）；连击占比（≤2 帧回抽）F 43.1% → **J 21.9%**。
三级火箭：**E** 让模型看得见上一步动作 → **F** 把无差别按键惩罚加到 −0.003 → **J** 只罚 ≤3 帧的回抽。

**③ 听指挥的鲁棒性**：G0 三档极差 **18.6pp**（锚点 0.963 / 跟点 0.919 / 自由 0.777）
→ I **0.9pp**（0.934 / 0.934 / 0.925）。部署时上层指挥稀疏或沉默的那些时段不再是短板。

### 已经定下来的（别再重开）

| 结论 | 出处 | 一句话理由 |
|---|---|---|
| `frame_skip = 1` | C | 2 帧一判只是把频率封顶，**按每次决策算比 1 帧还爱换方向**，还砍掉一半反应精度 |
| `key_press = -0.003` | D→E→F | ×3 才见效；再加码边际变差 —— 要治形状得用 J 那种定向惩罚 |
| 小怪也无敌 | 裁定 09-20 | 防目标污染：躲弹模型不该长出「去打怪」的议程 |
| 课程采样默认开 | H | +1.5～3.8pp，且消掉了 G0 的平台期 |
| 评测意图钉死成规范跟点档 | I | 训练意图换了，尺子不能跟着换 |
| 连击指标阈值固定在 ≤2/≤3 | J | 不跟 `reward.quick_frames` 走，否则换个惩罚参数历史数字全废 |
| 敌人速度特征直接转默认 | G（取消立项） | 快速敌人撞自机的情形本就少，不值一条 2.5h |

### 还没解决的

1. **曲线到 u3500 仍未走平**（I2 的 r3 一路爬到 0.909），轮数还不够；但 r2 已在 0.92–0.95 震荡。
   ⚠ **09-20 更正：「轮数不够」这个推断被 LR 退火混淆了，不成立** —— 见「更正：J 的代价是噪声」第二节。
2. **卡池六成已被学会**（74 个起点死亡率中位 0.030、45 个低于 5%），需要新的难度来源 —— rank 3 进训练最直接。
3. **评测集没有 5–6 关**，而最难的三个训练起点全在第 5 关 —— 在优化一个没有度量的方向。
4. **神穿没治**：贴弹 `<12px` 占比从 F 的 21.6% 一路涨到 I2 的 29.1%，J 只降到 26.3%。
   J 治抖不治贴，对症的是 backlog 里的「判定点随机增大」。
   **09-20 更新：病根找到了 —— 活命建立在精确到帧的操作上**（执行层最短保持 3 帧，r3 撑过 0.85 → 0.33）。
   惩罚改得了习惯、去不掉能力；对症的是把人手的限制写进环境（N1 / N2），优先级排到 L / M 前面。
5. **分档统计（`ep/{follow,anchor,free}/*`）在 I 那轮是错位的**（已修），修复后还没有一轮真正用上。
6. **近一半的死亡发生在贴边处，成因是躲弹时被一路顶到墙上**（09-20 诊断，见「诊断记录：死亡是怎么发生的」）。
   贴边只占 1.8% 的时间，每帧死亡风险却是平时的 47 倍；特征里几乎没有「墙」。这是目前已知的最大单一死因。

## P0 探测（2026-09-16）

- **目的**：新机器装得上、跑得动、能不能学。
- **配置**：`configs/probe.toml`（`probe/cards` 7 张，6 训 1 评 `th06_s1_b4` × 16 局）；ranks [2]；reward 默认（拟人项全 0）。
- **机器**：Vast.ai RTX 4090 + EPYC 7742（255 逻辑核），4096 env × 63 线程，8.6 万帧/s。
- **结果**（`runs/v4090/probe-d0e254c57cc2-20260916-054858`，260 次更新）：评测撑过 0 → 0.50，跟点 0.005 → 0.555，
  到达用时 300 → 62 帧，贴边 0.93 → 0.17；贪心方向变化 11.4/s、shift 5.2/s；训练采样时约为随机水平（50/s、28/s）。
- **结论**：能学；贴边是中段过渡形态，跟点塑形自己拉回来，不需要 `edge_hug`。卡池太窄、评测太薄，调 reward 没有依据 ⇒ 先补卡池（TH06 1–3 关 45 张）与评测集（9 卡 × 32 局）。

## A 基线（2026-09-17）

- **问题**：当前模型在真卡池上能学到什么程度、在哪里停住。
- **配置**：`configs/exp-a-baseline.toml`（默认值 + 2000 次更新、4096 env、eval_every 200）。代码 `c14f9e4`。
- **机器**：同 P0 型号，63 线程，7.8 万帧/s；更新占 44%、env 23%、H2D 14%；GPU 利用率 58%。
- **结果**（`runs/20260917-030045-exp-a`，best = u2000）：

  | 更新 | 帧 | 撑过 | 跟点 | 到达 | 方向/s | shift/s | 贴边 |
  |---|---|---|---|---|---|---|---|
  | 400 | 1.05 亿 | 0.677 | 0.399 | 101 | 26.3 | 8.1 | 0.260 |
  | 1000 | 2.62 亿 | 0.892 | 0.738 | 38 | 19.3 | 4.9 | 0.028 |
  | 2000 | 5.24 亿 | **0.938** | **0.765** | 35 | 23.7 | 7.1 | 0.015 |

  - 训练卡撑过 0.939 vs 留出卡 0.938：**无泛化差距**。
  - 最弱：`th06_s1_b4` 0.84、`th06_s2_mb1` 0.88；`th06_s3_w02`（高速弹）撑过 1.00 但跟点只有 0.59（优先躲）。
  - **抖动**：贪心方向变化在各 checkpoint 间 19–42/s 来回跳 ⇒ reward 对「停 / 晃」无所谓，抖多少随机漂。
    补测 best.pt（CPU，`eval-best-split-radius.json`）：**点内 25.0/s、点外 23.2/s**，按键 31.3/s ⇒ 抖动不是「停在点上原地晃」，而是全程都在抖（推翻了立项 D 时的猜测）。
  - 采样熵 2.89 → 2.47（几乎没降）；EV 0.94 → 0.66 → 0.70。
  - **操作回放**（`runs/20260917-030045-exp-a/replay/`，9 卡各第 0 局）：8 撑过 1 死。`th06_s2_mb1` 那局自机离开下半屏的指令点，
    一路跑到右上角 boss 出弹口附近（y≈40）被刚发出的弹打死——有「往上逃」的倾向，待多看几局确认；`th06_s3_b5` 按键带逐帧换色，按键 48 次/秒。
- **结论**：作为后续基线。能力够用，问题在操作频率完全不像人。

## C frame_skip = 2（2026-09-17）

- **问题**：每 2 帧决策一次，能否降抖动、加速训练、不掉能力。
- **配置**：`configs/exp-c-fs2.toml`：`frame_skip 2`、1000 次更新（同 5.24 亿帧预算）、gamma 0.995 → 0.990、hold 0.01 → 0.02（让「按秒」的视野与跟点收入和 A 一致）。代码 `c14f9e4`。
- **结果**（`runs/20260917-051018-exp-c`，best = u1000）：撑过 0.872、跟点 0.659、到达 52 帧、贴边 0.053、方向 13.5/s、shift 5.3/s；14.6 万帧/s。
- **对比 A**（`runs/cmp-ac/`）：
  - 同帧数：A 全面更好。
  - 同墙钟（约 64–67 min）：A u1000 = 0.892 / 0.738，C 最终 = 0.872 / 0.659 ⇒ 吞吐快 1.9 倍被「每帧学到得少」抵消。
  - 降抖只是封顶：方向变化占上限的比例 C 45%（13.5 / 30）、A 40%（23.7 / 60），按决策步算没变安静。
  - C 到最后还在涨（0.83 → 0.87），更长时间可能追近，但没有理由为此换掉 A。
- **结论**：不采用，下一批底子用 `frame_skip = 1`。抖动要靠 reward 治。

## D 按键惩罚（2026-09-17）

- **问题**：给按键一个很小的代价，能否让策略停止无意义的抖动，同时不伤活命与跟点。
- **配置**：`configs/exp-d-keypress.toml` = A + `reward.terms.key_press = -0.001`（与 A 只差这一个键）。代码 `7c94e65`，32 线程，7.6 万帧/s，2h12。
- **量级**：A 贪心约 31 次按键/s → −0.031/s；跟点停留收入约 0.45/s、死亡 −10。
- **结果**（`runs/20260917-065105-exp-d`，best = u2000）：

  | 更新 | 撑过 A → D | 跟点 A → D | 方向/s A → D | shift/s A → D | D 按键/s | D 点内 / 点外方向/s |
  |---|---|---|---|---|---|---|
  | 800 | 0.844 → 0.882 | 0.727 → 0.725 | 39.7 → 13.9 | 6.1 → 3.9 | 13.7 | 11.4 / 22.0 |
  | 1200 | 0.889 → 0.861 | 0.747 → 0.748 | 42.3 → 24.9 | 6.4 → 4.3 | 26.9 | 29.7 / 22.1 |
  | 1600 | 0.906 → 0.899 | 0.754 → 0.763 | 42.4 → 16.7 | 11.0 → 3.3 | 15.0 | 14.6 / 22.0 |
  | 2000 | 0.938 → **0.920** | 0.765 → **0.762** | 23.7 → **19.5** | 7.1 → **3.5** | 17.8 | 18.7 / 23.4 |

  - 活命与跟点不受影响（撑过 −1.8 个百分点，288 局标准误约 1.5）。单卡 `th06_s3_b5` 0.91 → 0.75、`th06_s3_b7` 1.00 → 0.88，其余持平或更好（32 局/卡，标准误约 7 个百分点）。
  - **shift 切换减半且各 checkpoint 稳定在 3–4/s；方向变化中后期均值 32 → 19/s，但仍在 14–29 之间来回跳。**
  - 点外方向变化在每个 checkpoint 都是 22–23/s 不动，降的只是点内那部分（且不稳定）。
  - 训练采样熵 2.476 → 2.443，采样策略的抖动（方向 50/s、shift 25/s）基本不变。
- **结论**：惩罚本身不伤能力，但对方向几乎无效。**原因（用户指出、代码核实）**：v1 观测里 `player.focus` = 上一帧是否按住低速
  （`stg-rl encode.rs:64`），所以模型看得到上一步的 shift、学得会「不切 shift」；方向完全看不到（只有位置，特征化里的自机速度是
  「假设朝指令点走」算的），按键惩罚对方向而言取决于一个不可观测量，等于随机扣分。多个方向几乎等价时 argmax 随弹的微小移动来回翻 ⇒ 抖。

## E 观测加上一步动作（2026-09-17）

- **问题**：让模型看到自己上一步的动作，按键惩罚能否像压 shift 一样压住方向抖动。
- **配置**：`configs/exp-e-prevact.toml` = D + `featurize.name = "danger_topk_v2"`（与 D 只差这一个键）。
  v2 = v1 + `player` 向量追加上一步动作 one-hot（方向 9 + 低速 1，共 13 维）；动作 id 取智能体坐标系（镜像前），新局首步为 0。
  模型按 spec 自适应（参数 282 451 → 283 091），旧 checkpoint 不兼容，从头训练。部署侧 C 端天然知道自己上一步动作。
- **判读**（和 D 叠图）：
  1. 方向变化降到与 shift 同一量级，且 checkpoint 间不再来回跳；
  2. 撑过率、跟点不明显下降；
  3. 点外方向变化也要降——只降点内说明点外那部分可能是真闪避；
  4. 看回放 GIF：该变向时是否变慢（过度依赖「继续上一步」）。
- **若失败**：方向仍抖 → `key_press` ×3（−0.003）另立一个实验；撑过率明显下降 → 看 GIF 找是否反应迟钝。
- **结果**（`runs/20260917-094100-exp-e`，代码 `2440bb1`，32 线程 7.7 万帧/s，best = u2000；对比图 `runs/cmp-de/`）：

  | 更新 | 撑过 D → E | 跟点 D → E | 方向/s D → E | shift/s D → E | 按键/s D → E | E 点内 / 点外方向/s |
  |---|---|---|---|---|---|---|
  | 600 | 0.837 → 0.760 | 0.662 → 0.708 | 18.3 → 11.7 | 5.2 → 3.6 | 18.9 → 11.6 | 10.1 / 17.4 |
  | 1200 | 0.861 → 0.899 | 0.748 → 0.750 | 24.9 → 14.5 | 4.3 → 3.1 | 26.9 → 14.3 | 15.7 / 16.0 |
  | 2000 | 0.920 → **0.931** | 0.762 → **0.771** | 19.5 → **13.0** | 3.5 → **2.7** | 17.8 → **10.8** | 12.4 / 16.5 |

  - 判读 1：方向变化 −33%，u600 起稳定在 11.7–14.5/s，**不再来回跳**；但离「个位数」还有距离。
  - 判读 2：撑过、跟点都略好（撑过 +1.1 个百分点，在噪声内）。单卡 `th06_s2_w06` 0.97 → 0.78，其余持平或更好（D 掉的 `s3_b5` 回到 0.94）。
  - 判读 3：点外方向变化也降了（23.4 → 16.5/s），不只是点内。
  - 神穿基线（首次有）：擦弹 1.78/s，离弹边缘 < 4 px 占存活帧 0.3%，< 12 px 占 13.7%。
  - 训练采样策略仍接近随机：方向 48.5/s、shift 24.6/s，熵 2.416（上限 2.89）；每局按键惩罚 −1.04 基本都来自探测噪声。
- **结论**：看得到上一步动作后，惩罚确实开始作用在方向上，而且让贪心策略稳定下来、不伤能力 ⇒ **v2 特征化保留为后续底子**。
  剩余抖动的候选解释：① 惩罚太小，相对价值估计噪声仍接近平局；② 熵系数 0.01 把策略压得很平，贪心 argmax 在几个接近的 logit 间翻；
  ③ 一部分是真闪避（点外 16.5/s）。下一步先做不花 GPU 的诊断再立实验。
- **诊断：滞回扫描**（本机 CPU，`eval_ckpt --hysteresis τ`：最优动作 logit 比上一步动作高出超过 τ 才换；结果 `runs/…-exp-e/hyst/`）：

  | τ | 撑过 | 跟点 | 方向/s（点内 / 点外） | 按键/s | shift/s | 贴边 | 擦弹/s |
  |---|---|---|---|---|---|---|---|
  | 0（= argmax） | 0.920 | 0.770 | 11.7（10.8 / 16.8） | 10.2 | 2.8 | 1.7% | 1.87 |
  | **0.5** | **0.927** | 0.761 | **6.6（6.2 / 8.8）** | 7.5 | 2.1 | 2.2% | 1.87 |
  | 1 | 0.868 | 0.754 | 7.5（7.5 / 7.1） | 9.7 | 2.1 | 2.8% | 1.92 |
  | 2 | 0.819 | 0.728 | 7.4（7.7 / 6.2） | 10.3 | 1.2 | 4.2% | 2.01 |
  | 4 | 0.618 | 0.526 | 4.7（3.3 / 7.4） | 7.0 | 0.4 | 9.6% | 2.20 |

  - **τ = 0.5 把方向变化砍掉 44%（11.7 → 6.6/s），撑过率不掉**（点外 16.8 → 8.8/s 也砍半）⇒ 约一半的变向是 logit 差 < 0.5 的「接近平局来回翻」，扔掉不伤躲弹。
  - τ ≥ 1 开始伤活命，方向变化却不再降（按键反而回升、贴边与擦弹上升）⇒ 剩下约 6–7/s 的变向是躲弹真正需要的，强压只会让它被卡住后大幅补救。
  - 单卡 `th06_s2_w06`（随机弹）对滞回最敏感：0.91 → 0.78（τ 0.5）→ 0.16（τ 4）。
  - 回放 GIF（无滞回）：`runs/20260917-094100-exp-e/replay/`。

## F 按键惩罚 ×3（2026-09-18）

- **配置**：`configs/exp-f-keypress3.toml` = E + `key_press = -0.003`。代码 `3cde3a6`，32 线程 6.5 万帧/s，2h29。
- **结果**（`runs/20260918-030737-exp-f`，best = u2000；下表为**旧评测集**，与 E 可比）：

  | 指标 | E | **F** |
  |---|---|---|
  | 撑过 | 0.931 | **0.938** |
  | 跟点 | 0.771 | 0.764 |
  | 方向/s | 12.95 | **6.57（−49%）** |
  | 点内 / 点外 | 12.4 / 16.5 | 5.3 / **12.1** |
  | shift/s | 2.69 | **0.75** |
  | 按键/s | 10.85 | **4.76** |
  | 擦弹/s · <4px | 1.78 · 0.3% | 1.83 · 0.4% |

  - **判读 1 达成**：方向变化落进 6–7/s，且**不需要滞回**——E 要靠部署侧 τ=0.5 才有 6.6/s，F 自己学会了。
  - **判读 2 达成**：点外 12.1/s，仍明显高于滞回压出的 8.8/s 下限 ⇒ 没有伤到真闪避。
  - **判读 3**：对 F 再扫滞回（新评测集）：τ=0 → 7.5/s（撑过 0.947）、τ=0.5 → 5.3/s（0.938）、τ=1 → 4.7/s（0.931）。
    还能再砍 29%，但代价是撑过 −1 个百分点；比 E 的 −44% 小得多 ⇒ 惩罚基本学到位，滞回降为可选。
  - 训练采样熵 2.416 → 2.231、采样方向 48.5 → 43.8：更强的惩罚确实压低了采样策略的乱按。
  - 单卡（旧集）：`s1_b4` 0.91 → 0.69 是唯一明显退步（32 局，标准误约 8 个百分点），但在**新评测集**上是 0.84 → 0.88，
    大概率是噪声，G 继续盯。
- **结论**：**`key_press = -0.003` 定为后续默认**。抖动这条线到此可以收：方向 6.6/s、shift 0.75/s、按键 4.8/s，
  已进入「熟练玩家」量级；剩余的靠部署侧滞回可选再降。

### 立项时定的判据（09-17）

- **问题**：E 的滞回扫描说明约一半变向是「logit 差 < 0.5 的平局来回翻」，扔掉不伤活命。加大惩罚能否让策略**自己**学会平局时保持上一步。
- **配置**：`configs/exp-f-keypress3.toml` = E + `reward.terms.key_press = -0.003`（与 E 只差这一个键）。
- **量级**：E 贪心约 10 次按键/s → −0.03/s（跟点停留收入约 0.46/s）；训练采样每局约 1000 次按键 → 每局约 −3（死亡 −10），比 D/E 更可能压低采样熵。
- **判读**（和 E 叠图；对照线是 E + 滞回 τ 0.5：方向 6.6/s、撑过 0.927）：
  1. F **不加滞回**时方向变化能否降到 6–7/s、撑过 ≥ 0.92——达到 = 策略学会了，部署可不依赖滞回；
  2. 点外方向变化别掉到 8/s 以下太多（E + τ0.5 是 8.8/s），再低就可能在压真闪避；
  3. 对 F 的 best.pt 也扫一遍 τ ∈ {0, 0.5, 1}：若 τ 0.5 仍能再砍很多，说明惩罚没学到位。
- **若失败**：活命掉 → 回到 −0.001，部署侧固定用 τ 0.5 滞回；抖动没降 → 考虑把滞回搬进训练（训练时就按滞回执行），或先做观测延迟。

## G 敌人速度（取消单独立项，2026-09-19 并入 G0）

- **问题**：v1/v2 的敌人特征把敌人当**静止**（`ve = -vp`），可敌人有体碰判定、撞上就死。一只每帧俯冲 3 px 的妖精，
  在「预测最近距离 / 几帧后到达最近点」里和一根柱子没区别。绝大多数道中卡的小怪都在动。
- **判别式**：第 2 关「妖精雨」`th06_s2_w06` 整段 0 弹、死因全是体碰，实验 E 在它上面的撑过率在 0.78–0.97 之间飘。
- **配置**：`configs/exp-g-enemyvel.toml` = F + `featurize.name = "danger_topk_v3"`（与 F 只差这一个键）。
  v3 = v2 + 敌人行追加 `(vx, vy)/8`，并用相对速度算最近接近。**env 不导出敌人 vx/vy**
  （`stg_rl.OFFSETS["enemies"]` 只有坐标 / 判定框 / hp / flags / id），由 `envwrap.enemy_velocity` 按 `enemies.id`
  匹配上一帧差分；新出现的敌与新局第一步记 0；镜像时 vx 取反。实测：妖精雨差分出 3.0、第 1 关道中 2.0，与原文 `move_speed` 一致。
- **判读**：整体撑过率；重点看道中卡与体碰型卡；`th06_s2_w06`（留在训练池）的死亡应从「随机」变成可预测。
- **部署侧**：C 端要复刻同样的差分，记 stg-engine follow-ups D23#9。

## G0 新卡池基线（2026-09-19 立项）

- **为什么 F 不能直接当基线**：F 收工时卡池 45 张（TH06 1–3 关），现在 84 张
  （+5–6 关 25 张、+`ex_ins` 解禁 14 张）。评测集这 10 张没动、尺子是同一把，但**训练分布变了**，
  直接拿后续实验跟 F 比会把「卡池」和「新机制」混在一起。G0 = F 的配置 + 现在的卡池。
- **同时吃下三个不单独立项的改动**（都属于「默认值升级」，不是待验证假设）：
  1. **起点钳档**：`cards.training_ranks` 把 `allowed_ranks` 的「过滤」改成「钳到该卡可用的最高档」。
     `ranks = [2]` 原本把 **9 张只有 E/N 的卡整张丢掉**（含第 5 关咲夜的三张时停卡），现在钳成 rank 1 纳入，
     起点 65 → 74。评测 / 录像仍用严格的 `allowed_ranks`，语义不变。
  2. **特征化 v3 转默认**（敌人速度）：G 取消单独立项，理由是快速敌人撞自机的情形本就少，不值一条 2.5h。
  3. **`enemy_velocity` 瞬移守卫**：单帧位移 > 16 px 记 0 速。咲夜六张卡把时停窗口压平后 boss 每轮瞬移一次
     （最远 112 px），不守就会喂进 100+ px/帧的离群速度，还让最近接近算出「这敌人瞬间飞走了」。
- **配置**：`configs/exp-g0-newpool.toml`。**判据**：撑过 / 方向相对 F（0.938、6.57/s）落在哪；
  预期撑过略降（分布更难更杂）、泛化更好。这条是 H 与 I 的比较基准。

## H 起点难度采样（2026-09-19 立项）

- **问题**：74 个起点难度差一个数量级（弹量峰值 0 → 872），均匀采样把大半算力花在早就会了的道中卡上。
- **做法**：按每起点死亡率 EMA 加权，`w ∝ clamp(fail, 0.05, 0.95)^α`，归一化到均值 1 后再钳进 `[0.25, 4]`。
  接口全是现成的：env 在自动 reset **之前**把 `start_index` 写进缓冲（所以 `done≠0` 那步读到的是刚结束那局的起点），
  `set_start_weights` 从 Rust 一路暴露到 Python，下一次 reset 生效。口径见 `src/stgtrain/curriculum.py`。
- **`w_hi` 是关键的那道闸**：近乎必死的卡（`th06_s5_b9` rank 3 一屏 731 颗弹、`th06_s5_b1` 峰 872）
  会稳定拿最高死亡率，没有上限就会把采样吸干，而模型从里面学不到东西。
- **配置**：`configs/exp-h-curriculum.toml` = G0 + `[curriculum].enabled`。
  **判据**：整体撑过是否上升，且评测里密档那几张（`s3_w12` / `s2_b6` / `s3_mb3`）是否明显改善；
  看 `curr/w_max` 是否长期贴着上限（贴着 = 上限在起作用，可能要调 `alpha`）。

## G0 / H 结果（2026-09-19，RTX 4090 + EPYC 7702P，threads 32）

两条各 5.24 亿帧、约 2 小时、吞吐 70–73k 帧/s。评测集未变（10 卡 × 32 局），尺子同一把。

| | 撑过 | 跟点 | 方向/s | 连击 | 临危/s | 平时/s | 贴边 | <12px |
|---|---|---|---|---|---|---|---|---|
| F（45 卡池） | 0.938 | 0.764 | 6.57 | 43.1%※ | — | — | 2.5% | 13.9% |
| G0（84 卡池） | 0.912 | 0.749 | 8.82 | 46.1% | 11.58 | 5.74 | 3.1% | 20.6% |
| H（+课程） | **0.950** | 0.766 | 8.40 | 47.5% | 11.47 | 5.43 | 1.7% | 20.7% |

※ F 的 43.1% 是事后用 `eval_ckpt` 在 CPU 上补测的（它训练时还没有这几个指标），与上面两行的 GPU 数字不完全同尺。

**① 卡池扩张的代价是 −2.6pp**（F 0.938 → G0 0.912），与预期一致：84 张里新进来的多是 boss 符卡，
弹量分布更密（`<12px` 占比 13.9% → 20.6% 说明贴着弹过的时间明显变多）。方向频率从 6.57 涨到 8.82
也主要是分布更凶所致——临危 11.58/s vs 平时 5.74/s，差 2 倍。

**② 课程采样是明确的赢**：+3.8pp，且**反超了卡池更简单的 F**。分卡看，增益全在难卡上：

| 卡 | 弹峰 | G0 | H | Δ |
|---|---|---|---|---|
| th06_s1_b4 | 187 | 0.72 | 0.91 | **+0.19** |
| th06_s2_b6 | 385 | 0.81 | 0.94 | **+0.12** |
| th06_s3_b5 | 433 | 0.91 | 1.00 | +0.09 |
| th06_s3_mb3 | 602 | 0.97 | 1.00 | +0.03 |
| th06_s3_w12 | 700 | 0.94 | 0.88 | −0.06 |

（其余 5 张两边都满分或持平。评测卡不在训练池里，所以这是泛化增益，不是刷分。）

**③ H 到 u2000 还没收敛**：评测曲线 `0.044 → 0.688 → 0.775 → 0.809 → 0.869 → 0.903 → 0.928 → 0.934 → 0.956 → 0.950`
单调爬升；而 G0 从 u1400 起就在 0.90–0.92 震荡。**下一批应该把 `total_updates` 加到 3000–4000**。

**④ 课程内部**：`fail_mean` 0.82 → 0.08，`w_max` 从 u1300 起**长期贴着上限 4.0**，`w_min` 0.25 → 0.53；
末轮 `fail_max` 只有 0.376（没有「不可学的卡」把采样吸干，w_hi 这道闸此刻是在压缩差距而不是救命）。
训练侧死亡率 H 0.204 > G0 0.150，正是「多练难的」应有的样子。
→ 可考虑把 `w_hi` 抬到 6 或调 `alpha`，但**先加长训练更值**（③ 的头寸更大）。

**⑤ 抖动没有改善**：连击占比 43% → 46–47%，方向频率反而上升。这是卡池变难的连带结果，
不是课程的锅（G0 和 H 的连击占比几乎相同）。J（只罚 ≤2 帧回抽）仍然该做，基准用 G0/H 的 46–47%。

## I 自由躲弹混合意图（2026-09-19 立项）

- **问题**：训练里模型永远被一个每 120–300 帧换位置的目标圈拉着跑。部署时上层指挥没这么密，
  「给个大致位置、别催你动」才是常态。
- **依据**（下面「诊断记录」那节的实测，同一模型同一评测集）：固定锚点 **0.972** > 随机目标 **0.931** >
  目标锁自机 **0.760**（贴边 21%）。所以「不逼着移动」要写成**锚点**而不是**无锚点**——
  锁自机等于抽掉位置先验，模型会自己漂到边上送死。
- **做法**：`intent.mixed_v1`，每局开局抽一档、整局不变：`follow` 60%（现状）/ `anchor` 30%
  （开局抽一个点、整局不刷新）/ `free` 10%（目标锁自机，只留一小撮做鲁棒性）。
  锚点与自由档的倒计时设成 NEVER，`refreshed` 不再触发（那个信号喂的是 reach_frames 与分段，对这两档没意义）。
- **测量口径**：新增 `[eval].intent`，**把评测意图钉死成规范的跟点档**，训练意图换了尺子也不跟着换；
  锚点 / 自由档的数字用 `eval_ckpt --intent fixed_point_v1 / follow_player_v1` 单独跑。
  训练侧 `ep/{follow,anchor,free}/*` 分档汇总（`ep/in_r_frac` 与 `reach_frames` 在后两档是退化指标，别看总表那一行）。
- **配置**：`configs/exp-i-freemix.toml` = G0 + `[intent].name = "mixed_v1"`。
  **判据**：锚点 / 自由两档撑过相对 G0 提升多少，且**跟点档不退化**——不能为了自由躲弹把听指挥的能力弄丢。

## I 混合意图结果（2026-09-19）

I 启动于课程转默认**之前**，所以它是 **G0 + 混合意图**（不含课程），与 G0 严格单变量可比。

| | 撑过 | 跟点 | 方向/s | 连击 | 临危/s | 平时/s |
|---|---|---|---|---|---|---|
| G0 | 0.912 | 0.749 | 8.82 | 46.1% | 11.58 | 5.74 |
| H | **0.950** | 0.766 | 8.40 | 47.5% | 11.47 | 5.43 |
| I | 0.934 | **0.772** | **5.99** | **41.8%** | 8.23 | 3.28 |

**最硬的那条证据不是撑过率，是抖动**：方向频率 8.82 → 5.99（−32%），**比卡池更简单的 F 还低**（6.57），
而且**十张卡无一例外全降**（6.4→3.4 / 9.0→7.1 / 12.5→8.7 …）。撑过 +2.2pp 落在 320 局的噪声里，
全盘一致的频率下降则不可能是噪声。

**「为了自由躲弹把听指挥的能力弄丢」没有发生，反而相反**：跟点率 0.772 是三条里最高。
推测是锚点档（30%）教会了「停在一处稳住并躲」，而跟点局里大部分时间本来就是在目标点附近驻留 ——
这个能力直接迁移，表现为驻留时不再来回抠。分卡看，赢在 boss 卡（s1_b4 +0.22、s2_mb1 +0.09、
s3_b5 +0.09），输在两张最需要不停重定位的密集道中卡（s3_w12 −0.09、s3_w02 −0.06）。

**三种意图下的补测（CPU，各 10 卡 × 32 局；I 真正被造出来要解决的场景）**：

| | 撑过 | 方向/s | 连击 | 贴边 |
|---|---|---|---|---|
| G0 锚点（`fixed_point_v1`） | 0.963 | 7.81 | 46.1% | 1.2% |
| I 锚点 | 0.934 | 4.49 | 42.6% | 0.5% |
| G0 自由（`follow_player_v1`，目标锁自机） | **0.777** | 4.17 | 32.3% | 29.4% |
| I 自由 | **0.925** | 1.76 | 29.2% | 27.1% |

**自由档 +14.8pp**，是这一批里最大的单项改善。把三档并排看才是真正的故事：

```
G0：锚点 0.963 · 跟点 0.919 · 自由 0.777    极差 18.6pp  ← 严重依赖「有人给点」
I ：锚点 0.934 · 跟点 0.934 · 自由 0.925    极差  0.9pp  ← 给不给点都一样能活
```

G0 只在「有明确目标可追」时强，上层一沉默就塌到 0.777；I 把这个依赖消掉了 —— **部署时指挥
稀疏或干脆没有的那些时段，模型不再是短板**。代价是锚点档 −2.8pp（那本来是最容易的一档，
且 G0 的 0.963 是「训练分布正中靶心」的特例），是好交易。

两个副作用值得记：① **贴边没治好** —— 自由档下两个模型都有 27–29% 的时间贴着边
（跟点/锚点档只有 0.5–1.2%），I 活下来靠的不是不往边上飘，而是飘过去之后躲得更好；
真要治得给自由档一个隐式位置约束（`edge_hug` 现在是 0）。② **贴弹变多**（自由档 `<12px`
17.2% → 23.4%）：I 更敢擦着弹过，这是撑过率涨的来源之一，也是「神穿」方向的负面信号 ——
J 治的是抖动不是贴弹，若 I2/J 之后这个比例继续涨，就该动 backlog 里的「判定点随机增大」。

⚠ **本 run 的 `ep/{follow,anchor,free}/*` 三行是废的**（三档数值几乎完全相同）：`envwrap.step` 里
`intent.reset(ended)` 会给结束的 env 抽下一局的模式，而 `intent_mode` 在它之后才读，于是刚结束那局
被打上了下一局的标签 —— 三档独立抽样，分组等于随机切一刀。**只坏了报表，不影响训练**
（意图在局内照常生效），评测数字与总体曲线仍然有效。已修（`e3a0f62`）并押了判别式测试。

## J 只罚回抽（2026-09-19 立项，与 I2 并行）

- **问题**：`key_press` 是无差别惩罚，压水平不挑形状；D→E→F 一路加码边际在变差，还会把有用的移动一起压掉。
  实测四成以上的变向发生在上一次变向后 **≤2 帧**（60Hz 下 ≤33ms）——那不是反应，是振荡。
- **做法**：新 reward 项 `quick_change`，只在「上一个方向保持 ≤ `reward.quick_frames` 步就又换」时扣分，
  阈值 **3 帧（50ms）**；阈值之外一分不扣，所以不压制正常走位，只掐振荡。
  `dir_hold`（距上次变向多少步）由 `envwrap` 统一算一份，reward 与逐局统计共用，避免两处算岔。
- **系数 −0.01 的量级**：I 的方向 5.99/s、连击约四成 ⇒ 约 2.5–3.5 次/s 被罚，合计 −0.03/s，
  与 `key_press` 的 −0.05/s 同量级（hold 的收入是 +0.6/s）。没动静就按 F 的经验 ×3。
- **指标**：`quick_frac`（≤2 帧，与历史同口径）与新增 `quick3_frac` / `quick3_per_s`。
  **指标阈值固定不跟 reward 参数走**——否则换个惩罚参数，历史数字就全废了。
- **为什么不是 `frame_skip = 2`**（用户 2026-09-19 提议）：实验 C 做过，撑过 0.872 vs A 0.938，
  而且**按每次决策算它比 A 还爱换方向**（占上限 45% vs 40%）——frame_skip 只是把频率封顶，
  没让策略变安静，同时把反应精度全局砍半。要结构性封顶该用 sticky actions（见下面 backlog）。

## I2 / J 结果（2026-09-19，两条同机并行）

※ 并行跑的墙钟：单条 47.8k / 47.1k 帧/s，合计 94.9k = 单跑 70k 的 **1.36 倍**，每条 5.33h / 5.41h
（单跑约 3h40）。与事前按「GPU 那 40% 的 update 会串行化」估的 1.4 倍吻合。

| | r2 | r3 | overall | 跟点 | 方向/s | 连击≤2 | ≤3 | ≤3 次/s | 临危/s | 平时/s | <12px |
|---|---|---|---|---|---|---|---|---|---|---|---|
| H（2000 轮） | 0.950 | —※ | — | 0.766 | 8.40 | 47.5% | — | — | 11.47 | 5.43 | 20.7% |
| I2 | **0.947** | **0.909** | 0.928 | **0.787** | 5.94 | 42.1% | 54.5% | 3.41 | 8.13 | 2.83 | 29.1% |
| J | 0.928 | 0.884 | 0.906 | 0.770 | **3.55** | **21.9%** | **30.7%** | **1.16** | 4.78 | 1.93 | 26.3% |

※ H 训练时评测集还没有 rank 3；事后用它的 best.pt 补测得 r2 0.934 / r3 0.844（CPU，比 GPU 低 1–2pp）。

**① 叠加成立，但增益主要在 r3 而不是 r2。** I2 的 r2 0.947 与 H 的 0.950 基本持平，
r3 0.909 则比 H 的 0.844（CPU，折算 GPU 约 0.86）高约 5pp —— 即**对未见难度的泛化变好了**，
而训练分布内已经接近这套卡池的上限。跟点率 0.787 是历来最高，抖动保住了 I 的水平（5.94 vs I 的 5.99）。

**② J 完成了它的任务**：连击占比 42.1% → **21.9%**（腰斩），被直接惩罚的 ≤3 帧回抽
3.41/s → **1.16/s（−66%）**，方向总频率 5.94 → 3.55/s（−40%）。
**代价是撑过 r2 −1.9pp / r3 −2.5pp**（⚠ 09-20 更正：这是 best 对 best 的噪声，末 4 次均值持平，见下面「更正」）。3.55/s 相当于平均每 17 帧（0.28s）换一次方向 —— 进入人类区间了。

**③ 代价拆不出结构**（2026-09-20 更正）：一开始按 r2 看像是「钱花在密集道中卡上」
（`s3_w12` −0.16、`s2_mb1` −0.12），但 r3 的方向完全相反 ——
`r2`：道中 4 张平均 **−0.047**、boss 6 张 **+0.000**；`r3`：道中 **+0.000**、boss **−0.042**。
每组 32 局、单卡 1σ 约 5pp ⇒ **这是噪声，不是落点**。总代价约 2pp 本身也才 1.5σ。

**④ 曲线到 u3500 仍未走平**（I2 的 r3：0.100 → … → 0.903 → 0.909），
说明 3500 轮**还是不够**；但 r2 已经在 0.92–0.95 之间震荡，继续加长主要买的是 r3。
（⚠ 09-20 更正：末段上爬是 LR 线性退火的必然结果，不能当「没学完」的证据，见下面「更正」第二节。）

### 第一次拿到逐起点课程数据（`curriculum.jsonl`）

I2 末轮 74 个起点的死亡率 EMA：**中位 0.030、均值 0.050，45/74 低于 5%** —— 这套卡池
对现在的模型已经有六成是「学会了」的。最难的几个全是第 5 关：

| 起点 | 死亡率 | 权重 | 见过局数 |
|---|---|---|---|
| `th06_s5_b4` | 0.344 | 4.00（贴上限） | 29611 |
| `th06_s5_b1` | 0.241 | 3.35 | 18858 |
| `th06_s5_w06` | 0.176 | 2.45 | 22688 |
| `th06_s3_b3` | 0.174 | 2.42 | 24071 |

最难的 10 个起点吃掉 26.5% 的局（均匀应为 13.5%），集中度约 2×。
**但真正卡住低端的是 `fail_floor = 0.05` 而不是 `w_lo`**：45 个起点的死亡率都低于 5%，
一律被钳成 0.05 ⇒ 拿同样的权重 0.70，`w_lo = 0.25` 从未生效。要更锐就降 `fail_floor`（如 0.01）或抬 `alpha`。

⚠ **最难的三个起点全是第 5 关，而评测集里一张 5–6 关的卡都没有** —— 我们在优化一个没有度量的方向。

## 更正：J 的代价是噪声；「曲线未走平」被 LR 退火混淆（2026-09-20）

更正的是「I2 / J 结果」的 ②（撑过代价 −1.9 / −2.5pp）与 ④（3500 轮还是不够），以及总表 J 行、阶段性总结里的同一说法。
③ 当时已经发现「代价按卡型拆不出结构」，这里把话说完：**不是拆不出结构，是代价本身就不存在**。

### 一、best 对 best 的 2–3pp 差不是信号

②的数字是拿两条 run 各自的 `best.pt` 比出来的。但同一条 run 内，相邻评测点之间什么都没改，撑过率自己就这么摆：

```
I2 overall：… 0.877 → 0.856 → 0.903 → 0.870 → 0.881 → 0.912 → 0.928
J  overall：… 0.842 → 0.873 → 0.856 → 0.908 → 0.884 → 0.902 → 0.906
```

相邻点 ±3pp 是常态（I2 u2500→u2750 一步掉 3.3pp，J u2750→u3000 掉 2.4pp）。三层噪声叠在一起：
① 每档 320 局，标准误约 1.5pp；② `best.pt` 是 14 次评测取最大，两条 run 各自抽中了多少运气不一样；
③ 全部实验都是单种子。**两条 run 的 best 差 ≤3pp，落在同一条 run 自己的摆幅之内。**

换成最后 4 次评测（u2750 / 3000 / 3250 / 3500）取均值，每档 1280 局：

| | r2 撑过 | r3 撑过 | 跟点 (r2) | 方向/s (r2 / r3) | 连击≤2 (r2 / r3) | <12px (r2 / r3) |
|---|---|---|---|---|---|---|
| I2 | 0.926 | 0.870 | 0.792 | 5.79 / 6.72 | 40.0% / 44.3% | 24.2% / 32.3% |
| J | 0.924 | **0.876** | 0.774 | **3.13 / 3.66** | **18.9% / 23.7%** | 22.4% / 30.1% |

**撑过 r2 −0.2pp、r3 +0.6pp —— J 在活命上没有可测的代价。** 抖动减半、贴弹略降是真的（效应几十个百分点，远超噪声）；
唯一站得住的代价是跟点率 −1.8pp。口径注意：4 个点共用评测种子、相邻 checkpoint 也相关，
所以 0.8pp 的名义标准误偏乐观；但两条 run 吃的是同一套偏差，互比公平。

**结论**：① **J 转默认**（`quick_change = -0.01`）；② K（×3）的立项理由要改 —— 不是「再花几个点买抖动」，
而是「J 还没碰到代价开始出现的那个点，弹性未知」；③ **今后比撑过率一律用末 4 次均值**（已写进「约定」）。

**回头看哪些旧结论受影响**：凡是靠 ≤3pp 的撑过差立住的都要打折 ——
D 的 −1.8pp、E 的 +1.1pp、F 的 +0.7pp（当时就标了「噪声内」，没问题）；I 的 +2.2pp（当时也标了）。
**不受影响的**：H 的 +3.8pp（分卡增益集中在难卡、且曲线形状不同：G0 平台震荡 vs H 单调爬升）、
C 的 −6.6pp、I 的自由档 +14.8pp、所有抖动类结论。

### 二、「u3500 仍未走平 ⇒ 轮数不够」不成立

`anneal_lr = true`：LR 从 3e-4 **线性退到 0**（`ppo.py:208`，`frac = 1 − (iter−1)/total`）。
所以**不管 `total_updates` 设多少，最后一段都会因为 LR 变小、策略变稳而上爬** —— 末段在涨不是「还没学完」的证据。

直接证据：同是 u2000，H（LR 已退到 0）r2 = 0.950，I2（LR 还剩 43%）r2 = 0.916；I2 跑完 3500 轮 r2 = 0.947，与 H 持平。
**3500 轮在 r2 上没有多买到任何东西。** r3 比 H 高的那约 5pp 分不清是混合意图还是加长带来的
（I2 同时改了这两样，H 又没有 r3 的训练期曲线）。⇒ **单纯加长不值得立项**；真想测长度，要同配置只改 `total_updates` 跑一对。

## 诊断记录：死亡是怎么发生的（2026-09-20）

起因是一轮关于模型结构的讨论（三个 encoder 要不要合成一个大的、密度图要不要加一张自机周围的细图、
完全无记忆好不好）。先量再动：**J 的 best.pt（u2750）× 评测集 20 组 × 32 局 = 640 局，跟点档，本机 CPU**，
共 75 次死亡。三个脚本都是包在评测循环外面的一次性诊断，没改仓库，**尚未入库**（方法写在下面，够重写）。

⚠ **口径**：单个 checkpoint、单种子、75 次死亡。分卡的数字（个位数）只能看方向；总量上的两个结论
（漏弹不重要、贴边很重要）效应都在一个数量级以上，站得住。

### 一、引擎判中弹后约 7 帧才给 `done`

命中帧（自机与某颗弹的边距 < 0）之后 6–8 帧 `done` 才置 1（决死窗口）。这段时间自机照常移动、
模型照常决策和领奖励，凶手弹则已经被清掉。两个后果：
① **找凶手要回溯**：在 `done` 前 12 帧里取「弹边距最小」的那帧当命中帧，凶手 = 那帧边距最小的弹；
直接拿 `done` 前一帧看，最近的弹在 20+ px 外，会误判成「非弹致死」。
② 训练里死亡的 −10 晚 7 帧到，这 7 帧的动作也被记了账。GAE 处理得了，影响不大，但凡是按帧对齐死亡的分析都要记得这个偏移。

### 二、top-K = 64 截掉了什么：截满很常见，但几乎不致死

特征化按「外推最近距离」取前 64 颗弹。先看截满频率（每组 4 局的小样本）：

| 卡 | 场上弹数均值 | 危险圈（≤128 px）内超过 64 颗的帧占比 r2 / r3 | 截满时第 65 颗的外推最近距离 p10 |
|---|---|---|---|
| `s2_b6` | 283 | 31% / 77% | 63–75 px |
| `s3_mb3` | 452 | 75% / 77% | 58–64 px |
| `s3_w02`（高速道中） | 162 | 45% / 47% | **21–31 px** |
| `s3_w12`（密集道中） | 236 | 51% / 53% | **13–26 px** |
| `s2_mb1`（螺旋） | 87 | 17% / 29% | 23–36 px |

多数卡上被截掉的弹在 60 px 开外，确实不用看 —— **加大 K 没有意义**。`s3_w02` / `s3_w12` / `s2_mb1` 三张是真漏：
10–22% 的帧里有此刻就在 48 px 内的弹没进网络。原因不是 K 小而是**排序键**：危险度按「假设自机全速朝锚点走」
外推，自机实际在躲弹、并不朝锚点走，按假想轨迹会擦身而过的弹排名很低。

**但它不致死**。对 69 次弹致死，沿凶手弹的速度反推它 N 帧前的位置，在那帧弹表里找 3 px 内的弹，看当时入没入选
（对照组 = 存活帧上随机抽的、一帧后离自机 < 24 px 的弹，4542 个）：

| 命中前 | t−3 | t−5 | t−10 | t−15 | t−30 |
|---|---|---|---|---|---|
| 凶手弹没入选 | 2.9% | 4.4% | **0%** | 4.8% | 24% |
| 对照组 | 0.3% | 0.3% | 1.0% | 1.7% | 4.3% |

命中前 3–15 帧内凶手曾经漏过的死亡只有 **5 / 69**（其中 4 次在 `s2_mb1` r3）。比例确实高于对照，
但**完全修好也只值约 0.8pp**（5 / 640）。⇒ 「为修漏弹而改排序键 / 加局部细图」不值得单独占一条 run。

### 三、47% 的死亡发生在贴边处，而且是被逼过去的

| | 贴边（离最近边 ≤ 16 px） | 不贴边 |
|---|---|---|
| 存活时间占比 | 1.8% | 98.2% |
| 死亡数 | 35 | 40 |
| 每万帧死亡风险 | 20.6 | 0.44 —— **差 47 倍** |

35 次贴边死亡的成因（阈值是诊断时定的，见各行）：

| 类型 | 次数 | 判据 |
|---|---|---|
| 被指挥到边上 | 4 | 目标点离边 ≤ 40 px 且自机在目标点 48 px 内 |
| 最后半秒被逼过去 | 13 | 半秒前离边还 > 32 px |
| 1–2 秒内逐步退过去 | 13 | 其余 |
| 赖在边上 | 5 | 死前 2 秒有一半以上时间贴边 |

- **轨迹**：离边距离的中位数，命中前 2 s 是 91 px → 1 s 前 37 → 半秒前 21 → 命中时 0。
- **早就放弃了指令点**：贴边死亡时自机离目标点的中位距离 153 px（35 次里 24 次 > 96 px）；不贴边的死亡只有 25 px。
- **与目标点留边无关**：目标点离边 ≤ 40 px 的帧占 22.5%，「被指挥到边上」的死亡却只有 4 次。
- **分卡**：`s2_mb1` r3（螺旋）17 次死亡里 12 次是被顶到墙上，`s1_b4` 13 次里 10 次贴边，`s3_w12` r3 7 次里 5 次。
  反例 `s3_b5`：16 次死亡只有 2 次贴边、凶手弹全程都在网络输入里 —— **另一种尚未查明的失败**。

**怎么理解**：这是退路判断的问题。每一步往远离弹流的方向让，单步看都对，连起来就把自己送进死角；
人在这种局面会横穿弹流回到中间。而**特征里几乎没有「墙」**：危险度只对弹和敌算，墙只通过自机绝对坐标
（x/192, y/192）间接进网络，往墙边退和往空地退在近身特征上完全一样。

**与 I 那节记的副作用是同一件事**：混合意图治好的是「飘到边上就死」，没治好「往边上飘」（自由档贴边率 27%）。
自由档的贴边时间是跟点档的十倍以上，部署时无目标点（th06nc mod 的 BYPASS 自 09-20 起默认 `free`）下这个死因只会更突出。

### 四、对模型结构那三个问题的结论（都未立项）

| 提议 | 结论 | 理由 |
|---|---|---|
| 三个 encoder 合成一个大的（token 间 self-attention） | 现在不做 | 上面的证据指向「看不见墙 / 没有退路感」，不是看见之后融合得不好；update 已占 44% 墙钟，self-attention 要掉吞吐，且与所有基线不可比。要判容量是否瓶颈，便宜的办法是补当年取消的 B（`d=32` / `d=128` 对照） |
| 粗密度图 + 自机周围的细局部图 | **有价值，但理由换了** | 不是为了修漏弹（只值 0.8pp），而是**加一个「墙」通道**：以自机为中心（如 16×16 格 × 8 px = 128 px = `d_max`），场外格子标成占据，墙和弹进同一种表示，退路在空间上直接可见；通道建议做成 t = 0 / 4 / 8 / 16 帧外推后的「未来占据」。要新特征化 v4 + 重导出；C 端不用改（图的输入还是原始表） |
| 加记忆（RNN / 帧堆叠） | 现在不做 | 弹带 `vx, vy`，对直线弹当前帧几乎就是完整状态；记忆买到的是弹幕节奏，更像上层指挥模型的职责。代价是循环 PPO + 截断 BPTT + CUDA graph 重做 + DLL 维护隐状态。**真要做，和「观测延迟」绑在一起**（延迟 d 帧后模型得靠最近 d 步动作推自己在哪），且优先用动作历史这种无状态输入 |

### 五、J 的活命建立在精确到帧的操作上

起因（用户 09-20）：模型很擅长「精确钻小缝」，看着稳如老狗，但那是建立在「精确向右移动 1 帧」这类人手做不到的操作上的。
先量依赖程度：**不重训，只在评测的执行层加人手约束**（J best.pt × 640 局，CPU；模型看到的「上一步动作」是实际执行的那个）。

| 执行层约束 | r2 撑过 | r3 撑过 | 变向/s |
|---|---|---|---|
| 无（基线） | **0.919** | **0.847** | 3.22 |
| 换方向后至少保持 3 帧（50 ms） | 0.628 | 0.331 | 2.91 |
| 至少保持 5 帧 | 0.309 | 0.081 | 2.60 |
| 至少保持 8 帧 | 0.125 | 0.009 | 2.29 |
| 变向随机延迟 `U{0..2}` 帧才生效（平均 1 帧） | 0.450 | 0.175 | 2.63 |
| 变向随机延迟 `U{0..4}` 帧 | 0.197 | 0.041 | 2.32 |

- **最轻的约束就崩**：3 帧比人能稳定做到的还松，r3 已掉 52pp；平均 1 帧的时机偏差就让 r2 减半。
  **十张卡无一例外**，连最稀的 `s1_w04` 也是（r3：1.00 → 0.47 → 0.06）—— 不只是钻缝时用，整套走位都建立在这上面。
- **与 E 的滞回扫描对照**：那次扔掉一半变向、撑过不掉 ⇒ 那一半是多余的。这次变向只从 3.22 降到 2.91/s，活命却崩了
  ⇒ **被禁掉的那一小部分操作正是它活命的关键**。
- **无约束下每段移动保持多久**（37 860 个移动段）：1 帧 13.1%、2 帧 12.8%、3 帧 10.1%、4–5 帧 16.0%、≥6 帧 48%。
  **四分之一的移动是 ≤2 帧的点按（0.63 次/s），三分之一以上 ≤3 帧。**
- **「方向变化 3.5 次/s ⇒ 进入人类区间」（阶段性总结 ②）要打折**：那个指标量的是换方向的**频率**，不是操作的**精度**。
  抖动确实治好了，但「像不像人手」它没量到。

**为什么 D → F → J 这条路到不了这里**：`quick_change` 是 −0.01，死亡是 −10。一次精确点按若能免掉一次死亡，
值 10、价钱 0.01，模型当然照付。惩罚压下去的只是「无所谓」的那些抖动（平局来回翻、驻留时抠位置）；
**惩罚改得了习惯，去不掉能力** —— 要去掉能力，得让环境里根本做不出来。

⚠ 口径：这张表量的是**依赖程度**，不是重训后的水平。模型没见过这种约束（起手一次点按、预期下一帧能停，
结果被锁着多走了 2 帧），是分布外输入；带约束重训的模型会提前规划，成绩会比表里好得多。
但它同时说明：**部署时在 DLL 里单方面加这层约束不可行**，等于把模型废掉。

## N1 / N2 手部运动层（2026-09-20 立项）

- **问题**：把人手的限制写进环境重训，模型能否在「做不出精确到帧的操作」的前提下重新学会活命，并因此自己留出余量。
- **目标（用户 09-17 拍板，见「待立项：拟人方向」）**：强但像人，**撑过率允许为此下降**。

### 为什么约束必须是随机的（用户 09-20 指出）

确定性的「至少保持 N 帧」只是把时间轴量化成 N 帧一格：模型照样知道「点一下 = 恰好 N 帧 = 恰好 N×速度 px」，
可以在粗一点的格子上**继续稳如老狗**。人手的限制不是格子粗，而是**结果不可控** —— 想点一下，出来的是 3 帧还是 5 帧
自己说了不算；想在这一帧按，实际落在前后一两帧。只有「同一个意图、结果有散布」才会逼出余量：
位移不确定 ⇒ 缝的宽度必须大于散布 ⇒ 窄缝自动变得不划算。而且余量会**自己按场景变**：高速移动时一帧误差对应 4.5 px、
要留大余量；静止时窄缝照样能待（人也是）。这比 backlog 里的「判定点随机增大」准 —— 那是一刀切，
连「静止待在窄缝里」这种人做得到的事也禁掉了。

### 运动层（训练包装层里，策略与 env 之间）

每个 env 一个小状态机；策略每帧输出「想按的方向」`want`，运动层决定实际执行的 `cur`：

```
状态：cur（正在执行的方向 0–8）· held（cur 已执行几帧）· L（本段的最短长度，换段时抽）
      pend / wait（待生效的方向与剩余延迟；N1 恒无延迟）

每帧：want == cur            → 清掉 pend
      want != cur 且 want != pend → pend = want，wait ~ U{delay_lo..delay_hi}     （新意图，新延迟）
      若 pend 存在：wait == 0 且 held >= L → cur = pend，held = 0，L ~ U{hold_lo..hold_hi}，清 pend
                    否则 wait = max(0, wait − 1)
新局：cur = 0（不动），held = 很大，L = 0，无 pend           （= 开局第一下不受限）
```

- **`L` 对模型不可见**：模型看得到 `held`（人知道自己按了多久），看不到「还要锁几帧」（人不知道手指什么时候真正抬起来）。
  这是散布的来源 —— 想点一下，实际 2–6 帧不等。
- **低速键不过运动层**（第一版）：只锁方向。**已知漏洞**：高速 4.5 / 低速 2 px 交替可以拼出细位移。
  盯 `shift_toggles_per_s`（J：r2 0.66 / r3 1.05），明显上升就在下一轮把低速键也接进来。
- **「上一步动作」记实际执行的**（`cur`，不是 `want`）；`key_press` / `quick_change` 与全部逐局统计也按执行的算 ——
  策略的 `want` 来回翻不花钱、也不算抖，只有真按出去的才算。`quick_change` 保留 J 的 −0.01 不动（单变量）。
- **镜像**：运动层作用在智能体坐标系的动作 id 上（镜像之前），只做相等与计数，天然对称。
- **随机数**：独立的 `torch.Generator`（种子由 run seed 派生），评测用固定种子，同一 checkpoint 两次评测逐位相同。

**参数（60 Hz，1 帧 = 16.7 ms）**：

| | `hold` = `U{2..6}` 帧 | `delay` |
|---|---|---|
| N1 | 33–100 ms，均值 67 ms。熟练玩家刻意点一下约 50–100 ms。**用户 09-20 拍板取 2–6**（比初稿的 2–5 散布更大） | 无 |
| N2 | 同 N1 | `U{0..2}` 帧，均值 17 ms。只建模**时机散布**，不是反应延迟（那是「观测延迟」，另立） |

N1 → N2 是单变量的一对：N1 回答「持续时间不可控」要多少代价，N2 再加「起手时机不可控」。
拆开跑是因为上面的诊断里这两种约束各自都能让 J 崩，不拆就分不清是谁在起作用。

### 特征化 v4 = v3 + `held`

`player` 向量追加一维 `min(held, 16) / 16`（13 → 14 维）。没有它状态不满足马尔可夫性：
同一个局面下「刚换完方向」和「已经走了 10 帧」能不能再换的概率完全不同。`held` 包装层里现成
（`envwrap.dir_hold`，给 `quick_change` 用的那个），接进 `RawObs` 即可。**不加 `pend`**：策略无状态、
每帧重新表态，延迟期间它只要继续想要同一个方向，`pend` 自然保持。

### 评测口径

- **主判据：带运动层评测**（`[eval].motor = "train"`，与训练同分布、固定种子），末 4 次评测均值。
  **对照线不是 J 的 0.92，而是 J 在同一个运动层下的成绩**（已补测，见下面「对照线」：
  N1 的层 0.400 / 0.122，N2 的层 0.184 / 0.056）。
- **新增指标**：`seg_le2_frac` / `seg_le3_frac`（移动段里 ≤2 / ≤3 帧的占比，**按执行的算**）、
  `motor/override_frac`（`want != cur` 的帧占比 = 运动层替它做主的频率）。
- **例行探针**（今后每个模型都测，CPU）：① 运动层关掉；② 加码到 `hold U{3..7}` + `delay U{0..4}`。

### 训练预算：3500 轮，与父实验 J 一致

09-20 讨论过砍到 2000（单跑 2h05 / 并行 3h05，而 3500 轮是 3h40 / 5h20）：依据是「更正」第二节 ——
I2 的 3500 轮在 r2 上没比 H 的 2000 轮多买到东西。**最后定回 3500**（用户，睡前挂机跑，时长不是约束）：
保持与 J 单变量；那条结论是没有运动层时得出的，运动层下的学习问题更难，先不在预算上省。
N 系列跑完后可以回头看 u2000 附近的评测点离终值多远，给下一批的轮数一个依据（注意 LR 退火的混淆）。

### 对照线：J 在同一运动层下（09-20，`eval_ckpt --motor`，J best.pt × 640 局，CPU）

| | r2 撑过 | r3 撑过 | 跟点 (r2) | 运动层做主的帧 | 移动段 ≤2 / ≤3 帧 | shift/s (r2 / r3) | <12px (r2 / r3) |
|---|---|---|---|---|---|---|---|
| J 无约束 | 0.919 | 0.847 | 0.774 | — | 25.9% / 36.0% | 0.66 / 1.05 | 22% / 30% |
| **N1 的层** `hold U{2..6}` | **0.400** | **0.122** | 0.757 | 2.5% | 3.9% / 11.1% | 1.25 / 1.39 | 17.5% / 20.7% |
| **N2 的层** `+ delay U{0..2}` | **0.184** | **0.056** | 0.749 | 5.9% | 1.7% / 6.2% | 1.16 / 1.33 | 14.4% / 18.0% |

结果文件 `runs/20260919-062442-j/motor/`。**这两行就是 N1 / N2 要超过的线**（不是 J 的 0.92）。

- **运动层只在 2.5% 的帧里替它做了主，活命就从 0.92 掉到 0.40** —— 那 2.5% 是它活命的关键。
- **shift 切换翻倍**（0.66 → 1.25/s），而这是同一个没重训的模型 ⇒ 方向被锁住时，低速键是它手里唯一还能立即生效的调节手段。
  「低速键不过运动层」这个漏洞重训后大概率真会被利用，**N1 / N2 出来第一个看 `shift_toggles_per_s`**。
- 贴弹占比下降（22% → 17.5%）不是好消息，只是死得早、贴不了那么久。
- **实现的交叉验证**：`--motor hold=3,3` 得 r2 **0.628** / r3 **0.331**，与第五节那份独立写的一次性诊断脚本
  （0.628 / 0.331）**逐位相同**。两份实现各写各的、同种子同结果 ⇒ 运动层的「最短保持」语义没写错
  （`delay` 那一半只有单测押运，没有第二份实现可对）。

### 判读（立项时定）

1. **带运动层的撑过**：成功 = r2 ≥ 0.85 且 r3 ≥ 0.70（参照：实验 C 的 2 帧确定性粒度掉了 6.6pp，这里更狠）。
   r2 < 0.75 ⇒ 约束太凶、学不动，放宽到 `hold U{2..4}` 重跑，不要硬加轮数。
2. **余量是不是自己长出来了** —— 这才是这条实验的本意：`close12_frac`（J：r2 22% / r3 30%）、`close4_frac`、
   擦弹/s 应明显下降。撑过回来了而贴弹没降 ⇒ 它找到了别的精确手段（先查低速键那个漏洞）。
3. **加码探针下平缓退化**：J 在轻约束下直接崩（0.92 → 0.63）；N 在比训练更凶的运动层下应只掉几个点。
   这是「不依赖精确操作」的直接证据，比任何频率指标都硬。
4. **关掉运动层不应更差**：更差说明它学会了依赖「被锁住」这件事本身（比如靠锁定期滑过某个位置），是坏味道。
5. **跟点率会掉，可接受**：停进 24 px 的圈本身就要精细操作。掉得太多（< 0.65）再考虑把 `hold_radius` 放大。
6. **贴边死亡占比**（47%，见第三节）顺带看：余量变大后被逼到墙上的频率可能变、也可能更糟，决定 M 还要不要单独跑。

### 代码（09-20 已写完，分支 `exp-n-motor-layer`，全量 255 项测试通过）

配置 `configs/exp-n1-motor-hold.toml` / `configs/exp-n2-motor-delay.toml`；运动层 `envwrap.MotorLayer`；
特征化 `danger_topk_v4`；探针 `eval_ckpt --motor off | hold=a,b[;delay=c,d]`；测试 `tests/test_motor.py`。
CPU 冒烟（3 轮、8 env、N2 配置）端到端跑通，新指标进了 `metrics.jsonl` 与终端评测块。**GPU 上还没跑过**。
下表是立项时列的清单，除最后一行（导出）外都已完成：


| 处 | 改动 |
|---|---|
| `config.py` / `base.toml` | `[motor]`：`enabled = false`、`hold = [2, 5]`、`delay = [0, 0]`；`[eval].motor = "train" \| "off"` |
| `envwrap.py` | `MotorLayer`（上面的状态机，张量化、无 Python 逐 env 循环）；`step` 里先过它再镜像、再送 env；`RawObs.dir_held`；新局复位 |
| `featurize/danger_topk_v4.py` | v3 + `held` 一维 |
| 逐局统计 / `evaluate.py` | 段长直方图 → `seg_le2_frac` / `seg_le3_frac`；`motor/override_frac` |
| `eval_ckpt.py` | `--motor off \| train \| hold=a,b[;delay=c,d]`（探针，也用来给 J 补对照线） |
| 测试 | 锁定期内不换、`L` 抽样范围、延迟期间改主意、`want` 撤回清 `pend`、新局复位、种子可复现、`prev_action` = 执行的、关掉时与现状逐位相同 |
| `export_onnx.py` | v4 多一个输入 `held` ⇒ **图版本 3**。实验成功再做 |

**部署侧（实验成功后）**：DLL 要跑**同一个运动层**（同参数，自己的 RNG）—— 不加的话模型虽然已经学会留余量，
但它想点 1 帧时真的会点出 1 帧，回放里照样不像人。对拍因此要改：日志里得同时有 `want` 与执行的动作
（协议 ACT 只有后者），对拍工具用执行的动作重建 `prev_action` / `held`、比的是 `want`。

### 没选的方案

- **确定性最短保持**（09-20 最初的提议）：见上，只是把格子变粗。真想验证「随机性是否必要」，
  可加一条 `hold = [3, 3]` 的对照，再用随机运动层去探它 —— 预期它会像 J 一样崩。先不花这条 run。
- **ALE 式 sticky action（以概率 p 重复上一步）**：散布是几何分布、长尾重（偶尔锁十几帧），不像人手；
  而且它惩罚的是「每一帧」而不是「每一次换方向」，走直线时也会被随机打断。
- **判定点随机增大**：一刀切，见上。留作 N 系列之后的补充手段。
- **在 J 上继续加大 `quick_change`（K）**：惩罚去不掉能力，见第五节。**K 取消**。

## 评测集沿革

### 第二版（2026-09-18）：按弹量分层、剔除无弹卡

第一版按标签挑，有两个毛病：**`th06_s1_w03` / `th06_s2_w06` 在 rank 2 下峰值为 0**（一张是发弹带 `!L` 粘滞前缀只在
Lunatic 生效，一张是只有体碰的妖精雨），等于两张白送的满分卡；而且 9 张全落在「中」档，**测不到高密度**
（全池最密的 `s3_b2` 均值 499、`s3_mb3` 396 当时都在训练池）。

第二版 10 张 × 32 局 = 320 局，按弹量分层（画像 `cards/density.json`，命令 `pipeline density`）：

| 档 | 卡 |
|---|---|
| 稀 | `s1_w04`(56) |
| 中 | `s1_b4`(113) `s2_w08`(75) `s2_mb1`(72) `s3_w02`(154) `s3_b5`(176) `s3_b7`(178) |
| 密 | `s2_b6`(241) `s3_w12`(223) `s3_mb3`(396) |

括号内是场上均值。训练起点 32 个；训练池仍保留 8 张「空」卡（只有体碰，作多样性）。

**⚠ 与实验 A–F 的撑过率不可直接比**：那批评测集含 2 张无弹卡、且没有密档，绝对值偏乐观。A–F 之间仍可互比。

### 第三版（2026-09-19）：每张卡加 rank 3

rank 2 见顶：G0/H/I 三条里 `s1_w04` / `s2_w08` 三家都是 1.00，`s3_b5` / `s3_mb3` / `s3_b7`
也各有两家顶格，十张里真正还在区分模型的只剩 `s1_b4`（0.72–0.94）与 `s2_b6`（0.78–0.94）。
10 张卡全都有 rank 3 档，且弹量跳得很凶（s1_b4 187→360、s2_b6 385→639、s3_w02 495→768），
于是每张加测 rank 3 → **20 组 × 32 局**。评测耗时只占总时长的一小部分（每次更新 3.74s × 2000 轮 ≈ 2.08h），翻倍付得起。

**实测验证了 rank 3 不是地板**（H 的 best.pt，20 组 × 32 局，CPU）：r2 撑过 0.934 → r3 0.844，
平均落差 −9.1pp，10 张里 7 张明显下掉（`s2_mb1` −0.19、`s2_b6` −0.16、`s3_w02`/`s3_w12`/`s3_b5` −0.12），
r3 上只剩 2/10 顶格（r2 是 4–5 张）。**天花板顶开了、又没顶到地板**，所以混合口径挑 `best.pt` 没问题。
旁证：r3 上抖得更凶（方向 9.99 vs 8.72/s、连击 51.2% vs 47.6%、贴弹 29.7% vs 21.4%），
与「卡池变难 ⇒ G0 比 F 抖」同源 —— **抖动强度由弹幕密度驱动，不是模型的固有属性**。

⚠ **I2/J 的 `overall` 会落在 0.88–0.90（H 同口径是 0.889），那不是退步而是换了尺子。**
判断进退看 `by_rank.r2`（H = 0.934），新头顶空间看 `by_rank.r3`（H = 0.844），
`overall` 只用于 I2 与 J 之间互比。

训练只用 rank 2（E/N 专属卡钳到 1），所以 **rank 3 测的是对未见难度的泛化**。
`evaluate` 新增 `by_rank` 聚合、训练日志新增 `eval/r2/*` 与 `eval/r3/*`：
**回看 A–I 要对 `by_rank.r2`，`overall` 已换口径**（best.pt 的挑选也改用混合口径）。

### 换版时的基线（2026-09-18，本机 CPU）

给 G 用的对照线（10 张、含密档；`eval_ckpt` 读的是**当前** splits 文件，不是 run 当时那份）：

| | 撑过 | 跟点 | 方向/s | shift/s | 按键/s | 擦弹/s | <12px |
|---|---|---|---|---|---|---|---|
| E best | 0.941 | 0.761 | 13.1 | 3.0 | 11.5 | 2.34 | 21.8% |
| **F best** | **0.947** | 0.753 | **7.5** | **0.9** | **5.4** | 2.33 | 21.6% |

新集比旧集略高（去掉了两张无弹卡、补了密档，但 F 在密档上也很稳）：单卡最低是 `s1_b4` 0.88 与 `s3_b5` 0.88，
最密的 `s3_mb3`（均值 396）E 1.00 / F 0.94、方向 14.5 → 11.3。

## 诊断记录：锚点 / 自由躲弹 / 激光（2026-09-18）

**三档锚点对比**（E 的 best.pt，9 张评测卡 × 32 局，本机 CPU，`eval_ckpt --intent`）：

| 意图 | 撑过 | 跟点 | 方向/s | 贴边 |
|---|---|---|---|---|
| `fixed_point_v1`（锚点固定场底中央） | **0.972** | 0.893 | 10.1 | 0.9% |
| `lower_half_uniform_v1`（现训练用：随机点，2–5 s 换） | 0.931 | 0.771 | 13.0 | 1.7% |
| `follow_player_v1`（锚点锁自机 = 无位置偏好） | 0.760 | — | 5.5 | **21.1%** |

- **随机换点（「瞎指挥」）的代价约 4 个百分点撑过率**；**没有锚点的代价约 21 个百分点**，因为会往边上飘（贴边 0.9% → 21%）。
  起作用的是**锚点本身（位置先验）**，不是随机性；随机只是训练期的探索/课程副产品。
- 推论：**「自由躲弹」不需要专门训练一个无指令模式**——部署时指挥模型空闲就发一个保守锚点（场底中央 / boss 正下方偏低）即可，撑过 0.97。
  原计划的「特征 v3 + 无指令段 + 贴边惩罚」降级为备选。
- 另记：方向变化 13.0 → 10.1 说明**有一部分抖动是「追点」造成的**，判读拟人指标时要和「躲弹造成的移动」分开看。

**待立项 · 指令分布更贴近实用**：现在是下半屏均匀随机、每 2–5 s 换一次，比指挥模型将来的用法激进。候选：拉长刷新间隔 /
新点靠近当前位置 / 混入长时间不动的锚点。**会改变训练分布、使既有基线不可比**，排在拟人主线之后。

**激光调查（2026-09-18，数原作 ECL）**：含激光指令的 sub 数 / 激光指令行数——
第 1 关 2/12、第 2 关 1/3、**第 3 关 0**、**第 4 关（帕秋莉）11/326**、**第 5 关（咲夜）0**、**第 6 关（蕾米）0**、第 7 关（Extra 芙兰）3/51。
⇒ TH06 的激光集中在第 4 关与 Extra；**第 5、6 关完全没有激光**，扩卡池不必等引擎。
决定（2026-09-18）：**先转写第 5、6 关**；引擎激光池仍是独立 milestone（stg-engine world-design §709 / follow-ups D23#10），
触发点改为「要打全作（含第 4 关 / Extra）或迁移验证显示激光是主要差距」。

模型侧结论（激光真来了怎么接）：**不要塞进弹的那条流**（点语义会丢掉线段几何）。正确做法是**再开一条集合流**：
每条激光一行 = 点到线段垂距 / 在哪一侧 / 自机速度与激光轴夹角 / 宽度 / **几帧后生效（预警倒计时）** / 剩余存活帧，
与弹、敌同构的 SetEncoder 共享 query；密度图另加一个「激光覆盖」通道；选前 K 用点到线段距离而不是点的最近接近公式。
预警线与现有「几帧后到达最近点」是同一类时间特征，不需要新机制。

## 裁定：卡池里的小怪也无敌（2026-09-20）

起因是个合理的质疑：真实游戏里玩家密集开火、小怪一波波死掉，道中压力远没有我们模拟的这么大，
那 J 在密集道中卡上的代价岂不是对着一个假需求？查下来事实比这更彻底 —— **两层都堵死了**：

| | 卡池现状 | 原作 |
|---|---|---|
| 小怪能否被打死 | `set_invuln(65535)`，**166 个敌 sub 无一例外** | 血量 50，打得死 |
| 自机火力 | `Loadout{power:0}` → 单发、每 4 帧 1 伤 = **0.25 伤害/帧** | 满火力秒掉一只妖精 |

即：就算去掉无敌，一只 50 血的妖精也要 **200 帧（3.3 秒）**不间断精准命中才死，而 `s2_w08`
每 10 帧下来一只 —— 压力照样掉不下去。（`enemies_killed` 事件确实在跳，但那是 `oob_guard`
的出界 `die()`，不是打死的。）

无敌这条规矩的来源：引擎侧 `docs/rl-card-pool.md` 只要求 **boss** 无敌（否则段长随模型站位变、
难度不稳定，评测不是同一把尺子）；转写契约把它推广到了每只小怪。

**裁定：保持现状，而且这是有意为之。** 理由不是「省事」或「上界更安全」，而是**目标污染**：
躲弹模型的职责只有「到指挥点 + 别死」，**火力站位是上层意图模型的职责**。小怪若能打死且收益明显，
躲弹模型会自己长出「往敌群靠能减压」的议程，在指挥点与敌群不一致时偷偷违抗指令。
无敌把两层的职责边界焊进训练分布里；训练压力是上界、部署时更低，误差方向安全。

**已知且接受的代价**：① 难度画像（`density.json` / 课程权重）系统性高估道中卡；
② 模型学不到「再撑 2 秒这波就没了」这种真实节奏。
**真实关卡（部署用的卡）另论** —— 那里小怪本来就该能打死，届时要先定自机火力档位与小怪血量的标定
（我们的引擎不是 TH06，照抄它的 50 血没有意义）。

## 下一批候选（2026-09-20）

| 候选 | 做法 | 为什么值得 | 代价 |
|---|---|---|---|
| **K = J ×3** | `quick_change` −0.01 → −0.03 | 测惩罚—代价的弹性：J 把连击压到 21.9%（仍有五分之一在 33ms 内），再加一档能否进 10% | 一条 3h40；J 的代价实测为零（见更正），所以弹性未知 —— 这正是要测的 |
| **rank 3 进训练** | `env.ranks = [2, 3]`（起点数 74 → 约 148） | 卡池六成已被学会（死亡率中位 0.030），这是最直接的新难度来源，且正对着 r3 指标 | 一条 3h40；训练分布变 ⇒ 又一条新基线 |
| **评测集补 5–6 关** | 加 3 张（如 `s5_b6` 时停 / `s5_b9` 高密度 / `s6_b4`），独立成组报 | 最难的三个训练起点全在第 5 关，而评测集一张都没有 | **不花 GPU**；老 10 张原样保留，可比性不受影响 |

K 与「rank 3 进训练」正交，可并行两条（同机 1.36 倍吞吐，约 5h15）。

### 09-20 诊断之后的调整：下一批 = L + M，K 让一轮

> ⚠ 同日被下面的「09-20 再调整」取代：先跑 N1 ‖ N2，L / M 挪到新底子上，K 取消。表里的做法与判据仍然有效。

| 候选 | 做法 | 为什么值得 | 代价 / 风险 |
|---|---|---|---|
| **L = J + rank 3 进训练** | `env.ranks = [2, 3]`，父实验用 J（它在活命上没有可测的代价，见「更正」） | 同上表第二行 | 一条 3h40 |
| **M = J + `edge_hug`** | `reward.terms.edge_hug = -0.02`（起步值；判定是离边 16 px 内，项本来就有、系数一直是 0） | 47% 的死亡在贴边处、风险 47 倍，是已知最大单一死因。量级：跟点档贴边 1.8% 的时间 ⇒ 约 −0.02/s 可忽略；自由档 27% ⇒ 约 −0.3/s，是 hold 收入的一半 | **纯配置**。风险：惩罚只教「别去那儿」、不教「怎么穿出来」，可能只是把死亡位置往里挪 16 px |
| 局部细图 + 墙通道（v4） | 见「诊断记录」第四节 | M 若只挪了死亡位置而没减少死亡，这才是让它看得见退路的办法 | 要写特征化 + 重导出；排在 M 的结果之后 |
| K = J ×3 | 同上表第一行 | 抖动已到 3.1–3.5 次/秒，边际价值低于贴边这条 | 让出这一轮的槽位 |

**M 的判据**（撑过率按末 4 次评测均值比）：① 贴边死亡占比从 47% 降下来 —— **要看死亡位置的分布，不只是贴边率**，
否则「贴着惩罚带外沿死」会被读成成功；② 总撑过上升；③ 自由档（`eval_ckpt --intent follow_player_v1`）贴边率从 27% 明显下降且撑过不掉。

### 09-20 再调整：先跑 N1 ‖ N2，L / M 挪到新底子上

「精确到帧」的诊断（诊断记录第五节）改变了优先级：运动层会改变整个策略的运作方式，
在没有运动层的模型上继续刷 r3（L）或修贴边（M），刷出来的基线很快就作废。

1. **下一批**：N1 ‖ N2 两条并行（要先写代码，见「N1 / N2 手部运动层」）。
2. **再下一批**：以 N 系列里胜出的那条为父，跑 M（`edge_hug`）与 L（rank 3 进训练）。
   M 要不要跑看 N 的判读第 6 条。
3. **K 取消**。局部细图 + 墙通道仍排在 M 的结果之后。

## 待立项：拟人方向（2026-09-17 记账，未排期）

**目标（用户拍板）：强但像人**——可以比大多数人强，但操作形态像熟练玩家：有反应延迟、不无意义抖动、躲弹留余量，
**不靠 0 帧反应和像素级精度「神穿」**。撑过率允许为此下降。

`key_press`（D）只治抖动：一次救命闪避的按键代价约 −0.002，死亡 −10，神穿不受影响。`frame_skip`（C）限制的是决策频率而非反应延迟，
33 ms 离人类很远，也治不了神穿。下面几条才是针对神穿的，都只改训练仓，**一次只立一个实验**：

| 候选 | 做法 | 预期作用 / 风险 |
|---|---|---|
| **神穿指标**（前置） | 评测加每秒擦弹次数（env events 第 1 列，擦弹半径 16 px）、每步离弹最近边缘距离的分布 | 没有指标就看不出下面几条有没有效；可对 A / D 的 `u*.pt` 用 `eval_ckpt` 补测 |
| 观测延迟 | 包装层环形缓冲，模型看 d 帧前的局面（先试 d = 8，约 130 ms） | 去掉 0 帧反应，逼出预判；部署侧 C 端须同样延迟 |
| 判定点随机增大 | 每局随机取「虚拟判定半径」= 真实半径 + [0, m] px；包装层发现弹进入虚拟半径就按死亡结算（done = 1，死亡奖励照给） | 学会留余量；引擎判定不变，只在训练胶水里判「虚拟死亡」；m 太大会学得过于保守、少跟点 |
| 操作手滑 | 以概率 p 执行上一步动作（sticky action，ALE 常用 p = 0.25），或以小概率换成随机动作 | 压精确到帧的操作；评测时是否也开要单独定（开 = 测鲁棒性，关 = 测能力）。**对抖动基本无效**（与 C 的 frame_skip 同理：机械地少执行切换，不让策略「不想切换」）；环境变随机还可能增加价值估计噪声、加重平局。实现注意：v2 观测里的「上一步动作」要记**实际执行**的动作，不是策略选的动作 |

> **09-20 更新**：「操作手滑」那一行写的「对抖动基本无效」针对的是「让策略不想抖」这个目标，仍然成立；
> 但目标已经换成「让策略**不能依赖**精确操作」，这时硬约束恰恰是对的工具 —— 已立项为 N1 / N2（手部运动层），
> 设计上不用 ALE 式 sticky action，理由见那一节的「没选的方案」。顺序相应改为：**运动层（N1 / N2）→ 观测延迟 → 判定点增大**。

建议顺序（09-17 原文）：神穿指标（✅ 2026-09-17 已加，E 起有数）→ 观测延迟 → 判定点增大 → 手滑（后两条可能与延迟部分重叠，看前一个的结果再定）。
观测延迟对抖动也有机制性帮助：延迟 d 帧时，比 d 更快的来回切换看不到效果、价值自然变低，配合按键惩罚更易学成「选定方向、保持一阵」。

