# 评测卡划分（spec §6）：这里列出的是留出卡，训练卡 = cards/ 下其余的卡。
#
# 2026-09-18 重建（第二版）。原则：
#   1. **必须有弹幕**——第一版按标签挑，漏看实际弹量，th06_s1_w03 / th06_s2_w06 在 rank 2 下峰值为 0
#      （前者发弹带 !L 粘滞前缀只在 Lunatic 生效，后者是只有体碰的「妖精雨」），等于两张白送的满分卡。
#   2. **按弹量分层**：稀 / 中 / 密都要有。第一版 9 张全是「中」，测不到高密度表现（全池最密的
#      s3_b2 均值 499、s3_mb3 396 当时都在训练池里）。弹量画像见 cards/density.json（`pipeline density`）。
#   3. 每关都有道中与 boss（含符卡）；标签覆盖 aimed/stream/ring/curve/spiral/random/fast/mixed。
#   4. 训练侧每个标签至少保留一张。
#
# ⚠ 与 2026-09-17 的实验 A–F 的撑过率不可直接比（那批评测集含 2 张无弹卡，且没有密档）。

[[eval]]
card = "th06_s1_w04"     # 第 1 关 道中 · 稀 · 峰值 96 / 场上均值 56
ranks = [2]
episodes = 32

[[eval]]
card = "th06_s1_b4"     # 第 1 关 符卡 · 中（与 probe/splits 相同，便于回溯） · 峰值 187 / 场上均值 113
ranks = [2]
episodes = 32

[[eval]]
card = "th06_s2_w08"     # 第 2 关 道中 · 中 · 峰值 205 / 场上均值 75
ranks = [2]
episodes = 32

[[eval]]
card = "th06_s2_mb1"     # 第 2 关 中 boss 非符 · 中（spiral） · 峰值 214 / 场上均值 72
ranks = [2]
episodes = 32

[[eval]]
card = "th06_s2_b6"     # 第 2 关 符卡 · 密（random dense） · 峰值 385 / 场上均值 241
ranks = [2]
episodes = 32

[[eval]]
card = "th06_s3_w02"     # 第 3 关 道中 · 中（fast） · 峰值 495 / 场上均值 154
ranks = [2]
episodes = 32

[[eval]]
card = "th06_s3_w12"     # 第 3 关 道中 · 密（640 弹池等效截止那张） · 峰值 700 / 场上均值 223
ranks = [2]
episodes = 32

[[eval]]
card = "th06_s3_b5"     # 第 3 关 boss 非符 · 中 · 峰值 433 / 场上均值 176
ranks = [2]
episodes = 32

[[eval]]
card = "th06_s3_mb3"     # 第 3 关 中 boss 符卡 · 密（spiral，全池第二密） · 峰值 602 / 场上均值 396
ranks = [2]
episodes = 32

[[eval]]
card = "th06_s3_b7"     # 第 3 关 boss 符卡 · 中（mixed） · 峰值 308 / 场上均值 178
ranks = [2]
episodes = 32
